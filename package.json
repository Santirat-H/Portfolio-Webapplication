{
  "name": "portfolio-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"web,api\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix web\" \"npm run start:dev --prefix api\"",
    "build": "concurrently --names \"web,api\" \"npm run build --prefix web\" \"npm run build --prefix api\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
